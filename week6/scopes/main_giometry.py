from geometry import circle
from geometry import rectangle

# Calculate areas
c_area = circle.area(5)
r_area = rectangle.area(4, 6)

print(f"Area of circle (r=5): {c_area}")
print(f"Area of rectangle (4x6): {r_area}")